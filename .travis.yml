language: java

matrix:
  include:
    - os: linux
      jdk: oraclejdk9
    - os: osx

script:
  - ./config/travis/run-checks.sh
  - time travis_retry ./gradlew clean checkstyleMain checkstyleTest test functional
  - if [[ "$TRAVIS_OS_NAME" == "linux" ]]; then netlify deploy -s $NETLIFY_SITE_ID -t $NETLIFY_ACCESS_TOKEN -p ./build; fi

addons:
  apt:
    packages:
      - oracle-java9-installer
      - npm

before_install:
  # pcre2 is needed for checkstyle to run in osx
  - if [[ "$TRAVIS_OS_NAME" == "osx" ]]; then brew update; fi
  - if [[ "$TRAVIS_OS_NAME" == "osx" ]]; then brew reinstall --with-pcre2 git; fi
  # do browser checks on linux
  - if [[ "$TRAVIS_OS_NAME" == "linux" ]]; then npm install netlify-cli -g; fi

before_cache:
  - rm -f  $HOME/.gradle/caches/modules-2/modules-2.lock
  - rm -fr $HOME/.gradle/caches/*/plugin-resolution/

cache:
  directories:
    - $HOME/.gradle/caches/
    - $HOME/.gradle/wrapper/
