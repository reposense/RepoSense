{
  "scripts": {
    "preinstall": "npx only-allow pnpm",
    "tests": "cypress run --browser chrome",
    "ci": "cypress run --env ci=true --browser chrome",
    "debug": "cypress open"
  },
  "devDependencies": {
    "cypress": "^14.0.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "cypress"
    ]
  }
}
