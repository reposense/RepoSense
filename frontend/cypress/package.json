{
  "scripts": {
    "tests": "cypress run --env local=true --browser chrome",
    "debug": "cypress open --env local=true",
    "ci": "cypress run --browser chrome"
  },
  "devDependencies": {
    "cypress": "^3.1.5"
  }
}
