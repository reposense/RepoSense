export enum FilesSortType {
  LinesOfCode = 'linesOfCode',
  Path = 'path',
  FileName = 'fileName',
  FileType = 'fileType',
}

export enum FilterType {
  Checkboxes = 'checkboxes',
  Search = 'search',
}
