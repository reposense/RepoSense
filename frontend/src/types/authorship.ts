export enum FilesSortType {
  LineOfCode = 'lineOfCode',
  Path = 'path',
  FileName = 'fileName',
  FileType = 'fileType',
}

export enum FilterType {
  Checkboxes = 'checkboxes',
  Search = 'search',
}
